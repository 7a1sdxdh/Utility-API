# 서버 포트 (로컬 실행 시에만 사용, Vercel에서는 무시됨)
PORT=3000

# 허용할 CORS Origin (콤마로 구분, * 는 전체 허용)
CORS_ORIGIN=*

# Rate Limit 설정
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60

# 외부 요청 타임아웃 (ms)
REQUEST_TIMEOUT_MS=10000

# ip-api.com 요청 시 사용할 언어
IP_API_LANG=en
